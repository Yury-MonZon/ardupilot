[project]
name = "pidff-simulator"
version = "0.1.0"
description = "ArduPilot PIDFF Simulator for tuning education (Roll/Pitch)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygobject>=3.44.0",
    "numpy>=1.24.0",
]

[dependency-groups]
dev = []
